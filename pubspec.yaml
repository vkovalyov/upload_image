name: upload_image
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  #network
  dio: ^5.0.2
  #statemanager
  flutter_bloc: ^8.1.1
  #di
  get_it: ^7.3.0

  image_picker: ^0.8.7+4
  url_launcher: ^6.1.10

  #for test
  mocktail: ^0.3.0
  bloc_test: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

